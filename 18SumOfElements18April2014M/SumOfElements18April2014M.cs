using System;

class SumOfElements18April2014M
{
    static void Main()
    {
        string[] array = Console.ReadLine().Split();
        int[] numbers = new int[array.Length];
        
        for (int i = 0; i < numbers.Length; i++)
        {
            Console.WriteLine(numbers[i]);
        }
    }
}